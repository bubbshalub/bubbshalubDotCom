export default function JoinUrl(
  key: string,
  value: string,
  replace: boolean,
  debug: boolean,
): null {
  // get URL
  const currentUrl = window.location.href;
  console.log();

  // add string to url

  // apply to new url

  // if replace == true
  //// replace url with new one

  return null;
}

function Console(enabled: boolean, type: string, message: string) {
  if (enabled == true) {
  }
}
